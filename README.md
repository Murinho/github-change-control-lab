# GitHub Change Control Lab

A disposable repository for learning GitHub PRs,
reviews, required CI checks, branch policies,
and merge strategies.

## Application

policy.py contains a simplified networking
egress policy.

## Tests

Run:

python -m unittest discover -s tests -v